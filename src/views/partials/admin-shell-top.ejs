<nav class="navbar navbar-expand-lg topbar border-bottom">
  <div class="container-fluid px-4">
    <a class="navbar-brand fw-bold brand-lockup" href="/">
      <span class="brand-next">Next</span><span class="brand-edge">AI</span>
      <span class="brand-sub">Customer Portal</span>
    </a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#topbarNav"
      aria-controls="topbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="topbarNav">
      <div class="ms-auto d-flex align-items-center gap-2">
        <span class="small text-secondary d-none d-md-inline"><%= pageTitle || "Admin" %></span>
        <a class="btn btn-sm btn-outline-primary rounded-pill px-3" href="/admin/customers">Customers</a>
      </div>
    </div>
  </div>
</nav>

<main class="container-fluid py-4">
  <div class="row g-4">
    <aside class="col-lg-3 col-xl-2">
      <div class="card border-0 shadow-sm sidebar-panel">
        <div class="card-body p-3">
          <p class="eyebrow mb-3">Navigation</p>
          <div class="list-group sidebar-nav">
            <a
              href="/"
              class="list-group-item list-group-item-action <%= activePage === 'home' ? 'active' : '' %>"
            >
              Dashboard
            </a>
            <a
              href="/admin/customers"
              class="list-group-item list-group-item-action <%= activePage === 'customers' || activePage === 'customer-form' || activePage === 'tokens' ? 'active' : '' %>"
            >
              Customers
            </a>
          </div>
        </div>
      </div>
    </aside>
    <section class="col-lg-9 col-xl-10">
